# Fokus Editor 開発フェーズ詳細解説

---

## Phase 0: 企画・要件定義

### 背景と課題

Astroブログの記事を書く際、MDXファイルの編集には以下の課題がありました：

- **Frontmatter（メタデータ）の手入力が面倒** - YAML形式で正確に書く必要がある
- **プレビューの確認が煩雑** - `npm run dev`でサーバーを立ち上げる必要がある
- **Git操作が別途必要** - エディタとターミナルを行き来する

### 技術選定の議論

| 選択肢 | メリット | デメリット |
|:--|:--|:--|
| 案A: PyQt6（Python GUI） | Python一本で完結、開発が速い | UIが古臭くなりがち |
| 案B: Tauri + React | モダンなUI、高パフォーマンス | 技術スタックが増える |

**決定理由**: UI品質とユーザー体験を重視し、**案B（Tauri + React + FastAPI）**を採用。
ブログと同じデザインシステムを使えるため、ブランドの一貫性も保てる。

---

## Phase 1: MVP開発

### 目標

「記事を作成・編集・保存できる」最小限の機能を実装

### 1.1 プロジェクトセットアップ

```bash
# Tauri + React の初期化
npm create tauri-app@latest fokus-mdx-editor
```

| 技術 | 役割 |
|:--|:--|
| Tauri | Rustベースの軽量デスクトップフレームワーク（Electronより軽量） |
| FastAPI | Python製の高速APIサーバー（バックエンド処理用） |

### 1.2 記事管理機能

| 機能 | 説明 |
|:--|:--|
| ファイル一覧表示 | サイドバーにMDXファイルをリスト表示 |
| 読み込み | MDXファイルをFrontmatterと本文に分離 |
| 保存 | 編集内容をMDXファイルに書き戻し |

### 1.3 エディタ機能（Monaco Editor）

**Monaco Editor**はVSCodeと同じエディタエンジンで、以下を実現：

| 機能 | 説明 |
|:--|:--|
| シンタックスハイライト | Markdown記法の色分け |
| 自動補完 | タグ入力時の候補表示 |
| ショートカットキー | iA Writer準拠のキーバインド |

### 1.4 Frontmatterエディタ

GUIフォームで以下を編集可能に：

| フィールド | 型 | 必須 |
|:--|:--|:--|
| title | string | ✓ |
| pubDatetime | datetime | ✓ |
| description | string | ✓ |
| tags | string[] | - |
| draft | boolean | - |
| featured | boolean | - |

---

## Phase 2: Git連携機能

### 目標

「エディタ内でGit操作を完結できる」

### 実装した機能

| 機能 | 説明 |
|:--|:--|
| 変更ファイル検出 | 未保存・未コミットの状態を視覚化 |
| コミットUI | メッセージ入力フォーム |
| プッシュ | ワンクリックでリモートに反映 |
| 自動メッセージ生成 | 変更ファイル一覧を自動挿入 |

### コミットメッセージの自動生成例

```
新しい記事を追加

変更ファイル:
- src/data/blog/new-article.mdx (追加)
- src/data/blog/existing-article.mdx (変更)
```

これにより、何を変更したかがコミット履歴から一目でわかるようになりました。

---

## Phase 3: UX強化・バグ修正

### 3.1 ライブプレビュー

| 機能 | 説明 |
|:--|:--|
| リアルタイムプレビュー | Markdown→HTMLを即座に変換 |
| スクロール同期 | エディタとプレビューが連動 |
| CSSスタイル | ブログと同じスタイルを適用 |

### 3.2 UX改善

| 機能 | 説明 |
|:--|:--|
| 自動保存 | 30秒ごとに自動保存 |
| 未保存警告 | ウィンドウを閉じようとすると確認ダイアログ |
| ダークモード | ブログのダークモードと同じ配色 |

### 3.3 バグ修正

#### Monaco Editor読み込み問題

| 項目 | 内容 |
|:--|:--|
| 症状 | 本番ビルドでエディタが「Loading...」のまま止まる |
| 原因 | Monaco EditorがCDNから読み込もうとするが、CSPでブロック |
| 解決策 | ローカルバンドルからロードするよう設定 |

```typescript
import { loader } from '@monaco-editor/react';
import * as monaco from 'monaco-editor';
loader.config({ monaco });
```

#### pubDatetime未来日時問題

| 項目 | 内容 |
|:--|:--|
| 症状 | 日本時間12:00入力 → UTCで21:00（翌日）で保存される |
| 原因 | タイムゾーン変換のロジックが逆 |
| 解決策 | FrontmatterEditorでローカル時間→UTC変換を正しく実装 |

---

## Phase 4: ビルド・配布

### 4.1 macOSアプリのビルド

```bash
npm run tauri build
```

→ `Fokus Editor.app` が生成される

### 4.2 ランディングページ（LP）の作成

| 項目 | 内容 |
|:--|:--|
| 日本語版 | https://shek-fokus.com/fokus-editor/ |
| 英語版 | https://shek-fokus.com/fokus-editor/en/ |
| 機能 | 言語切り替えボタン、レスポンシブ対応、OGP設定 |

---

## 各フェーズの工数目安

| フェーズ | 想定期間 | 実際の状況 |
|:--|:--|:--|
| Phase 0 | 1-2日 | 完了 |
| Phase 1 | 2-3週間 | 完了 |
| Phase 2 | 1週間 | 完了 |
| Phase 3 | 1週間 | 完了 |
| Phase 4 | 1週間 | 進行中（署名・自動更新は未実装） |

---

## 今後の予定

| 機能 | 優先度 | 状態 |
|:--|:--|:--|
| R2画像アップロード | 中 | 未実装 |
| 画像管理UI | 中 | 未実装 |
| WebP自動変換 | 低 | 未実装 |
| コード署名 | 低 | 未実装 |
| 自動更新機能 | 低 | 未実装 |
